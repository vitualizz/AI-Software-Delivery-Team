package memory_test

// EngramProvider and MCPCaller have been removed in Stage C (org-knowledge change).
// All memory provider tests for InMemoryProvider live in inmemory_test.go.
// This file is intentionally minimal — the package still compiles and exports
// Provider, Entry, EntryContent, and EntryType for use by the config validation layer.
